import { Table } from "../../../widgets";
import { Title } from "../../../shared";

export const Admin = () => {
  return (
    <section>
      <Title children="Admin Page" />
      <Table />
    </section>
  );
};
